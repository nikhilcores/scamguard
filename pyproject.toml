[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "scamguard"
version = "0.1.0"
description = "A simple tool to check suspicious messages for common scam signs."
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
